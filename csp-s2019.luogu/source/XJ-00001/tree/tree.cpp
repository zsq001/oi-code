#include<stdio.h>
#include<string.h>
int n,father[100];
char a[100],str[100];
long long n,k;
long long f[64];
int main()

{
	freopen("brackets.in","r",stdin);
	freopen("brackets.out","w",stdout);
	scanf("%||d",&n);
	for(int i=1,i<=m,i++);
	{
		scanf("%d",a[i]);
		if{a[i]};
		else if{a[i]=1};
		scanf("%||d %||d",&n,&k);
    	int a=1;
    	k++;
    
    
    for(int i=1,i<=n,i++)a*2;
    for(int i=1,1<=k,i++);
       {
       	int q=[%],p=int(1/4);
       	if{q=1||q=3[f/n]=4};
        else if{q=2}
       	
       }
		
	}
	printf("E");
	return 0;
}


       
