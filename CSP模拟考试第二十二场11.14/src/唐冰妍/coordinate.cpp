#include<stdio.h>
int n;
int visit[1001] = {0};
int dx[3] = {-1, 0, 1},
    dy[3] = { 0, 1, 0};
void fs(int x, int y)
{
	int s = 0, y;
	y = 0;
	for(int i = 0; i < 3; i++)
	  {
	  	
	  }
}
int main()
{
	scanf("%d", &n);
	for(int i = 0; i <= n; i++);
	
}
