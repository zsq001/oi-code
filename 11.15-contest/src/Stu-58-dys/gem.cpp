#include<stdio.h>
#include<string.h>
#include<math.h>
#include<algorithm>
#define ll long long int
using namespace std;
ll n;
int main()
{
	scanf("%lld%lld",&n,&m);
	for(i=1;i<=n;++i)
	{
		scanf("%lld",&a[i]);
	}
	for(i=1;i<=m;++i)
	{
		scanf("%lld",&p);
		if(p==1) 
		{
			scanf("%lld%lld",&l,&r);
			for(j=l;j<=r;++j)
			{
				for(o=l+1;o<=r;++o)
				{
					
				}
			}
		}
		else scanf("%lld%lld%lld%lld",&l1,&r1,&x,&y);
	}
	
}
